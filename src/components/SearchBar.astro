---

import CoolQS from "@components/QS/CoolQS.svelte";
import People from "@components/QS/People.svelte";

function handleSubmit(e) {

}
---
<div class="ab-quick-search">
    <div class="ab-qs-desktop">
        <form on:submit={handleSubmit}>
            <CoolQS client:visible/>
            <People client:visible/>
            <!-- TODO: place picker / promo code here ? -->
        </form>
    </div>
</div>

<style>
    form {
        display: flex;
    }
    .ab-qs-desktop {
        max-width: 1100px;
        box-sizing: border-box;
        min-height: 6rem;
        padding: .5rem 1.5rem 0 1rem;
        background-color: #fff;
        border-radius: .5rem;
        margin: 0 auto;
        box-shadow: 0 6px 24px 6px #00000013;
    }

    @media (min-width: 984px) {
        :global(.ab-header-banner+.ab-quick-search) {
            position:relative;
            margin-top: -46px
        }

        .ab-quick-search {
            margin: 3.5rem 2.5rem 2.25rem;
        }
    }
</style>